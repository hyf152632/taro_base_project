import Taro, { Component } from '@tarojs/taro'
import { View } from '@tarojs/components'

class User extends Component {
  config = {
    navigationBarTitleText: '商城'
  }
  render() {
    return(<View> cate page. </View>)
  }
}

export default User
