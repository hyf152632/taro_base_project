import Taro, { Component } from '@tarojs/taro'
import { View } from '@tarojs/components'

class User extends Component {
  config = {
    navigationBarTitleText: '我的'
  }
  render() {
    return(<View> user page. </View>)
  }
}

export default User
