import Taro, { Component } from '@tarojs/taro'
import { View } from '@tarojs/components'

class User extends Component {
  config = {
    navigationBarTitleText: '购物车'
  }
  render() {
    return(<View> cart page. </View>)
  }
}

export default User
